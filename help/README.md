# Help / reference

| File | Purpose |
|---|---|
| [`esp.txt`](esp.txt) | Historical external ESP/offset notes (CL-era). Live offsets: `Project/Core/Offsets.h`. |
| [`sdk.txt`](sdk.txt) | Large FrostDumper-style dump for grepping class/property names. Not used by the build. |
| [`SHARED_GATES.md`](SHARED_GATES.md) | Shared ESP admission/naming choke points — grep callers before editing. |

Do not delete `esp.txt` / `sdk.txt` during cleanup; they are reference archives.
