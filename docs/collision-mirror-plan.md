# [ARCHIVED] Collision Mirror Plan

This experimental AggGeom/collision-mirror plan was abandoned. Runtime data showed
zero usable AggGeom primitives for the current build, so no collision visibility
system is active.

The only planned visibility approach is now:

- `docs/lrts-visibility-plan.md`
- `docs/lrts-visibility-plan.html`

No implementation from this plan should be restored without a new review.